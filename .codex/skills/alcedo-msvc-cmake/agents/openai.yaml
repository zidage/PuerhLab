interface:
  display_name: "Alcedo MSVC CMake"
  short_description: "Run CMake through msvc_env.cmd"
  default_prompt: "Use $alcedo-msvc-cmake to configure, build, install, or test alcedo on Windows with scripts/msvc_env.cmd."

policy:
  allow_implicit_invocation: true
